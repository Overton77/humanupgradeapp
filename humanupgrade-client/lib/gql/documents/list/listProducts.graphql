query ListProducts($input: ProductSearchInput!) {
  products(input: $input) {
    total
    items {
      hybridScore
      product {
        ...ProductCardFields
        organization { id slug name }
      }
    }
  }
}
