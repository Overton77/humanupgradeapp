/**
 * Re-export of all enum types from the API's generated Prisma client.
 *
 * This file exists as a stable import path. The actual enum definitions
 * live in `humanupgradeapi/generated/enums.ts` and are regenerated
 * by `prisma generate` (see scripts in the root package.json).
 */
export * from '../../../humanupgradeapi/generated/enums.js'
